/* -----------------------------------------------------------------------------
 * #1: -sqrt(2)+sqrt(2) -> expt: Undefined: 0 to a negative exponent.
 * -------------------------------------------------------------------------- */

-sqrt(2)+sqrt(2);
0$

/* -----------------------------------------------------------------------------
 * #2: 2D-Display of -sqrt(2) wrong
 * -------------------------------------------------------------------------- */

/* No example for 2D-display */

/* -----------------------------------------------------------------------------
 * #3: -3*a/sqrt(2)+sqrt(2)*a+a/sqrt(2) not fully simplified
 * -------------------------------------------------------------------------- */
 
 -3*a/sqrt(2)+sqrt(2)*a+a/sqrt(2);
 0$
 